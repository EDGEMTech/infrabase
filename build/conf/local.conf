
# Infrabase - Main configuration

# auto.conf carries machine-derived overrides (currently IB_BOOT_CHAIN)
# regenerated by scripts/common/bblayers.sh:regen_boot_chain whenever
# build.sh / deploy.sh runs. Loaded right after this file so its
# unscoped assignments are visible to recipes without :linux/:torizon
# in their OVERRIDES (e.g. uboot).
include conf/auto.conf

BB_NUMBER_THREADS = "20"

# Main target physical/emulated hardware

# IB_PLATFORM ?= "virt32"
IB_PLATFORM ?= "virt64"
# IB_PLATFORM ?= "rpi4"
# IB_PLATFORM ?= "rpi4_64"
# IB_PLATFORM ?= "verdin-imx8mp"

# IB_PLATFORM ?= "x86-qemu"

# Linux General Configuration

# Available configurations
IB_CONFIG:linux:x86-pc ?= "x86_defconfig"
IB_CONFIG:linux:x86-qemu ?= "x86_qemu_defconfig"

# QEMU/virt64 configuration

# This configuration is used to start Linux alone, or on top AVZ
IB_CONFIG:linux:virt64 ?= "virt64_defconfig"
# IB_CONFIG:linux:rpi4_64 ?= "rpi4_64_defconfig"
IB_CONFIG:linux:rpi4_64 ?= "rpi4_64_nomodules_defconfig"
IB_CONFIG:linux:verdin-imx8mp ?= "verdin_imx8mp_defconfig"

# Configuration to start Linux as agency to support SO3 capsules
# IB_CONFIG:linux:virt64 ?= "virt64_soo_defconfig"

PREFERRED_VERSION_linux:virt32 = "6.12"
PREFERRED_VERSION_linux:virt64 = "6.12"
PREFERRED_VERSION_linux:rpi4_64 = "6.12-rpi4"
PREFERRED_VERSION_linux:x86-qemu = "6.12"
PREFERRED_VERSION_linux:verdin-imx8mp = "6.6-verdin"

# Preferred version for rootfs

# When the rootfs is fully built and user apps are deployed in it

PREFERRED_VERSION_rootfs-linux:virt64 = "1.0"
PREFERRED_VERSION_rootfs-linux:rpi4_64 = "1.0"
PREFERRED_VERSION_rootfs-linux:x86-qemu = "1.0"

PREFERRED_VERSION_buildroot:virt64 = "2024.11"
PREFERRED_VERSION_buildroot:rpi4_64 = "2024.11"

# Main ITS configuration.
#
# IB_TARGET_ITS = AVZ ITS basename (hand-written) → produces
#                 ${IB_PLATFORM}_avz.itb via mkimage
#
# AVZ ITS is platform-wide (single AVZ per platform). The e1c capsule
# ITS is auto-generated from a board-generic template (see
# meta-e1c/recipes-e1c/common/<platform>_e1c.its.in) into the fixed
# name ${IB_PLATFORM}_e1c.its when IB_E1C_ACTIVE="1" — capsule presence
# is signalled by meta-e1c-<variant>/conf/layer.conf and the substituted
# specifics (DTB, initrd, description) come from IB_E1C_DESC /
# IB_E1C_GUEST_DTB / IB_E1C_INITRD declared there.
IB_TARGET_ITS:linux:rpi4_64 ?= "rpi4"
IB_TARGET_ITS:linux:verdin-imx8mp ?= "verdin-imx8mp_avz"
IB_TARGET_ITS:torizon:verdin-imx8mp ?= "verdin-imx8mp_avz"
IB_TARGET_ITS:linux:virt64 ?= "virt64_avz"
IB_TARGET_ITS:torizon:virt64 ?= "virt64_avz"

# Boot chain selector. Three values:
#   "uboot"      : U-Boot only (QEMU -bios u-boot.bin). No ATF, no OP-TEE,
#                  no AVZ. Used for the purest bare bsp-linux/virt64 dev mode.
#   "atf+uboot"  : ATF (BL1+BL2+BL31) + U-Boot, no OP-TEE, no AVZ. Closer to
#                  verdin's structural SoC boot. Used for bare bsp-torizon
#                  on virt64 and for every bare build on verdin-imx8mp (the
#                  iMX8MP requires BL31 — SPL → ATF → U-Boot is hardware).
#   "full"       : ATF + OP-TEE + U-Boot + AVZ + e1c capsule. Forced by
#                  regen_boot_chain whenever -e1c <variant> is selected.
#
# Authoritative source: ${BUILDDIR}/conf/auto.conf, regenerated each
# invocation of build.sh / deploy.sh by
# scripts/common/bblayers.sh:regen_boot_chain (which sees both the
# capsule arg and the recipe name). Do NOT add override-scoped
# IB_BOOT_CHAIN:* entries here or in capsule layer.conf files —
# bitbake's override-conditional lookup would win over the auto.conf
# base in bsp-linux/bsp-torizon scopes while losing in uboot scope (no
# :linux/:torizon in OVERRIDES there), leaving the U-Boot defconfig
# selection out of sync with the do_itb / do_deploy boot chain.

# Preferred version for bsp

# To deploy from the host to the target via ssh/scp
PREFERRED_VERSION_bsp-linux:x86-qemu = "1.0"
PREFERRED_VERSION_bsp-linux:virt64 = "1.0"
PREFERRED_VERSION_bsp-linux:rpi4_64 = "1.0"

# U-boot bootloader
PREFERRED_VERSION_uboot:virt64 = "2022.04"
PREFERRED_VERSION_uboot:rpi4_64 = "2022.04"
PREFERRED_VERSION_uboot:verdin-imx8mp = "2024.07"

PREFERRED_VERSION_usr-linux:x86-qemu = "1.0"
PREFERRED_VERSION_usr-linux:virt64 = "1.0"
PREFERRED_VERSION_usr-linux:rpi4_64 = "1.0"

PREFERRED_VERSION_linux = "${PREFERRED_VERSION_linux:${IB_PLATFORM}}"

# *************** SO3 ***************

# Using the most recent version
PREFERRED_VERSION_so3:virt64 = "6.1.0"
PREFERRED_VERSION_so3:rpi4_64 = "6.1.0"

# Configuration for SO3

# IB_PLATFORM:so3 ?= "virt64"
# IB_PLATFORM:so3 ?= "virt32"
# IB_PLATFORM:so3 ?= "rpi4"
IB_PLATFORM:so3 ?= "rpi4_64"

IB_CONFIG:so3:virt64 ?= "virt64_defconfig"
# IB_CONFIG:so3:virt64 ?= "virt64_capsule_defconfig"
IB_CONFIG:so3:rpi4_64 ?= "rpi4_64_defconfig"

# Main ITS configuration
IB_TARGET_ITS:so3:virt64 ?= "virt64_so3"
IB_TARGET_ITS:so3:rpi4_64 ?= "rpi4_64_so3"

# This ITS is used to use SO3 as capsule
# IB_TARGET_ITS:so3:virt64  = "virt64_capsule"

# This ITS enables running SO3 on top of AVZ
# IB_TARGET_ITS:so3:virt64  = "virt64_avz_guest"

# *************** AVZ ***************
# AVZ hypervisor General Configuration
PREFERRED_VERSION_avz:virt64 = "6.1.0"
PREFERRED_VERSION_avz:verdin-imx8mp = "6.1.0"

# Configuration for AVZ
IB_CONFIG:avz:virt64 ?= "virt64_avz_defconfig"
IB_CONFIG:avz:verdin-imx8mp ?= "verdin-imx8mp_avz_defconfig"
# IB_CONFIG:avz:virt64 ?= "virt64_avz_soo_defconfig"

# Type of deployment (soft for virtual storage, hard for target hardware)
# !! BE VERY CAREFUL WITH THIS VARIABLE !!

# When deployed locally (for example on the PC where the build system is running)
# IB_STORAGE_MODE ?= "local"

IB_STORAGE_MODE:virt64 ?= "soft"
# IB_STORAGE_MODE:verdin-imx8mp ?= "hard"
IB_STORAGE_MODE:verdin-imx8mp ?= "http"
IB_STORAGE_MODE:rpi4_64 ?= "hard"
# IB_STORAGE_MODE ?= "remote"

# Physical device used when IB_STORAGE_MODE = "hard"
IB_STORAGE_DEVICE:rpi4 = "mmcblk0"
IB_STORAGE_DEVICE:rpi4_64 = "mmcblk0"
IB_STORAGE_DEVICE:verdin-imx8mp = "sda"

# HTTP deploy path: TEZI files are placed here for TEZI network auto-install
IB_HTTP_DEPLOY_PATH:verdin-imx8mp = "/var/www/html/verdin-imx8mp-prod"

# Size of the rootfs partition
IB_ROOTFS_SIZE ?= "2G"

# Target partition for the main rootfs

IB_ROOTFS_PARTITION:virt32 = "p2"
IB_ROOTFS_PARTITION:virt64 = "p2"
IB_ROOTFS_PARTITION:rpi4 = "p2"
IB_ROOTFS_PARTITION:rpi4_64 = "p2"
IB_ROOTFS_PARTITION:torizon = "p2"

IB_ROOTFS_PARTITION:x86-qemu = "p1"

IB_ROOTFS_PATH:linux = "${IB_DIR}/linux/rootfs"
IB_ROOTFS_PATH:so3 = "${IB_DIR}/so3/rootfs"
IB_ROOTFS_PATH:torizon = "${IB_TORIZON_DIR_PATH}/build/deploy/images/${IB_TORIZON_MACHINE_ID}"

IB_ROOTFS_METHOD ?= "buildroot"
# IB_ROOTFS_METHOD ?= "debootstrap"

# CPU core and toolchain according to the platform

IB_PLAT_CPU:virt32 = "arm"
IB_PLAT_CPU:rpi4 = "arm"

IB_PLAT_CPU:virt64 = "aarch64"
IB_PLAT_CPU:rpi4_64 = "aarch64"
IB_PLAT_CPU:torizon = "aarch64"
IB_PLAT_CPU:verdin-imx8mp = "aarch64"
IB_PLAT_CPU:x86-qemu = "x86"

IB_TOOLCHAIN:arm = "arm-linux-gnueabihf"
IB_TOOLCHAIN:virt64 = "aarch64-none-linux-gnu"
IB_TOOLCHAIN:rpi4_64 = "aarch64-none-linux-gnu"
IB_TOOLCHAIN:verdin-imx8mp = "aarch64-none-linux-gnu"

# Debug options
IB_USR_BUILD_TYPE = "Release"

# ARM Trusted Firmware platform
IB_ATF_PLAT:verdin-imx8mp = "imx8mp"
IB_ATF_PLAT:virt64 = "qemu"

# Platform-specific ATF build options
IB_ATF_EXTRA_OPTS:verdin-imx8mp = "IMX_BOOT_UART_BASE=0x30880000 SPD=opteed BL32_BASE=0xbe000000 BL32_SIZE=0x2000000 DEBUG=0 LOG_LEVEL=20"

# virt64: SPD=opteed only in "full" mode (BL2 then loads BL32 = OP-TEE from
# the FIP). In "atf+uboot" mode the FIP carries BL31+U-Boot only — leaving
# SPD=opteed makes BL2 hang on "Loading image id 4" because there is no
# BL32 entry to load. SPD must be dropped so BL2 jumps straight from BL31
# to BL33 (U-Boot).
IB_ATF_EXTRA_OPTS:virt64 = "${@bb.utils.contains('IB_BOOT_CHAIN', 'full', 'SPD=opteed ', '', d)}LOG_LEVEL=40"

# OP-TEE platform
IB_OPTEE_PLAT:verdin-imx8mp = "imx-mx8mpevk"
IB_OPTEE_PLAT:virt64 = "vexpress-qemu_armv8a"


# Platform-specific OP-TEE build options
# Verdin iMX8MP has 2 GB DRAM (0x40000000-0xBFFFFFFF)
# TZDRAM 30MB (0xbe000000-0xbfdfffff) + SHMEM 2MB (0xbfe00000-0xbfffffff) = top 32MB of 2GB DRAM
# CFG_TZDRAM_SIZE uses default 0x1e00000 (30MB) so SHMEM fits within DRAM (overflow at 0xc0000000!)
IB_OPTEE_EXTRA_OPTS:verdin-imx8mp = "CFG_TZDRAM_START=0xbe000000 CFG_DDR_SIZE=0x80000000 CFG_TEE_CORE_LOG_LEVEL=3 CFG_UART_BASE=0x30880000"

BB_NO_NETWORK_SANDBOX = "1"
BB_WARN_ONLY = "true"

# EXTRA_OVERRIDES enables fine-grained customization for the various IB recipes.
# EXTRA_OVERRIDES .= ":soo"
EXTRA_OVERRIDES .= ":lvgl"
# EXTRA_OVERRIDES .= ":lvgl:soo"

# TorizonOS-specific configuration variables

# IB_TORIZON_MACHINE_ID = "verdin-imx8mm"
IB_TORIZON_MACHINE_ID = "verdin-imx8mp"

# The location of the TorizonOS yocto subdir
# NOTE: changing it requires a full rebuild of TorizonOS
IB_TORIZON_DIR_PATH = "${IB_DIR}/torizon"

IB_TORIZON_MAIN_RECIPE = "torizon-docker"
# IB_TORIZON_MAIN_RECIPE = "torizon-minimal"

