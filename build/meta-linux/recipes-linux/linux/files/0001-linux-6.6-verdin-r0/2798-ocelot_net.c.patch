--- /home/rossierd/edgemtech/products/edgem1/edgem1/30/build/tmp/work/linux-6.6-verdin-r0/linux-6.6-verdin/drivers/net/ethernet/mscc/ocelot_net.c	2026-04-08 18:20:08.326974887 +0200
+++ ./drivers/net/ethernet/mscc/ocelot_net.c	2026-04-08 16:21:07.763081793 +0200
@@ -1744,9 +1744,16 @@
 
 	ocelot_port->phy_mode = phy_mode;
 
-	err = ocelot_port_configure_serdes(ocelot, port, portnp);
-	if (err)
-		return err;
+	if (phy_mode != PHY_INTERFACE_MODE_INTERNAL) {
+		ocelot_port->serdes = devm_of_phy_get(dev, portnp, NULL);
+		if (IS_ERR(ocelot_port->serdes)) {
+			err = PTR_ERR(ocelot_port->serdes);
+			dev_err_probe(dev, err,
+				      "missing SerDes phys for port %d\n",
+				      port);
+			return err;
+		}
+	}
 
 	priv = container_of(ocelot_port, struct ocelot_port_private, port);
 
