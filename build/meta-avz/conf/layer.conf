# Copyright (c) 2025-2026 EDGEMTech SA

BBPATH .= ":${LAYERDIR}"
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb ${LAYERDIR}/recipes-*/*/*.bbappend"

# append layer name to list of configured layers
BBFILE_COLLECTIONS += "avz"

LAYERSERIES_COMPAT_avz = "infrabase_core"
LAYERDEPENDS_avz = "avz"

# and use name as suffix for other properties
BBFILE_PATTERN_avz = "^${LAYERDIR}/"
BBFILE_PRIORITY_avz = "4"
